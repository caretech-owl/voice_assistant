# Simple Voice Assistant

This is still WIP...

## Install

```sh
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Usage

```sh
source venv/bin/activate
python run.py
```

After setup, the assistant is waiting for "Hey Jarvis". Current config defines a german voice assistant.